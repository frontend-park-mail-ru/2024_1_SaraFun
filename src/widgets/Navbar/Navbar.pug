include ../../shared/components/TextButton/TextButton.pug

mixin navbar(isAuth)
    .nav
        .nav__logo
            img(src='./Logo.svg', alt='logo')
        .main-nav
            ul
                if (isAuth)
                    li.nav-link
                        a(href='/feed') Главная
                    li.nav-link
                        a(href='/profile') Профиль 
                else 
                    li.nav-link
                        a(href='/login') Логин
                    li.nav-link
                        a(href='/signup') Регистрация
        if (isAuth)
            button#button-logout(type="button") Выйти из аккаунта
            //нужна кнопка профиля пускай пока будет просто квадрат-ссылку на /profile, куда в дальнейшем впишу фотографию
